<%= form_for(@question) do |f| %>
<% if @question.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@question.errors.count, 'error') %>
  prohibited this question from beign saved:</h2>
<ul>
  <% @question.erros.full_messages.each do |msg| %>
  <li><%= msg %></li>
      <% end %>
</ul>
</div>
<% end %>
<div class="field">
  <%= f.label :text %><br />
  <%= f.text_area :text, :size => '50x3' %>
</div>
<div class="actions">
<%= f.submit %>
</div>
<% end %>